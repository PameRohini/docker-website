apiVersion: apps/v1
kind: Deployment

metadata:
  name: yaml-nginx

spec:
  replicas: 3

  selector:
    matchLabels:
      app: yaml-nginx

  template:
    metadata:
      labels:
        app: yaml-nginx

    spec:
      containers:
        - name: nginx
          image: nginx